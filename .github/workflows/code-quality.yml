name: Code Quality

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    name: Lint code
    
    steps:
      - uses: actions/checkout@v7
      
      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true
          
      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::lintr, any::styler
          needs: check
          
      - name: Lint R code
        run: |
          if (!requireNamespace("lintr", quietly = TRUE)) {
            install.packages("lintr")
          }

          lint_results <- lintr::lint_dir("R/")
          print(lint_results)

          if (length(lint_results) > 0) {
            cat("Linting issues found. Please review and fix.\n")
            quit(status = 1)
          }
        shell: Rscript {0}
        
      - name: Check code style
        run: |
          if (!requireNamespace("styler", quietly = TRUE)) {
            install.packages("styler")
          }

          style_results <- styler::style_pkg(dry = "on")
          styled_files <- style_results$file[style_results$changed]
          if (length(styled_files) > 0) {
            cat("Code style issues found in the following files:\n")
            cat(paste0("  - ", styled_files, collapse = "\n"), "\n")
            cat("Run styler::style_pkg() to fix these issues.\n")
            quit(status = 1)
          }
        shell: Rscript {0}
        
  spell-check:
    runs-on: ubuntu-latest
    name: Spell check
    
    steps:
      - uses: actions/checkout@v7
      
      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true
          
      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::spelling
          needs: check
          
      - name: Check spelling
        run: |
          if (!requireNamespace("spelling", quietly = TRUE)) {
            install.packages("spelling")
          }

          spelling_errors <- spelling::spell_check_package()
          if (nrow(spelling_errors) > 0) {
            cat("Spelling errors found:\n")
            print(spelling_errors)
            cat("Add words to inst/WORDLIST or fix spelling.\n")
            quit(status = 1)
          }
        shell: Rscript {0}
