% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{with_logging}
\alias{with_logging}
\title{Evaluate an expression with error logging}
\usage{
with_logging(expr, log_file = "chaoticds.log", msg = NULL)
}
\arguments{
\item{expr}{An expression to evaluate.}

\item{log_file}{Character string giving the path to a log file. Defaults to
"chaoticds.log" in the current directory.}

\item{msg}{Optional message to record before evaluating `expr` for context.}
}
\value{
The result of evaluating `expr` if successful.
}
\description{
This helper evaluates `expr` and writes any warning or error message to
`log_file` before re-throwing the error. A custom informational `msg` can be
provided to annotate the log when evaluation begins. It is useful for scripts
that should record failures for later inspection while still stopping
execution.
}
\examples{
tmp <- tempfile()
try(with_logging(stop("oops"), tmp, msg = "example"))
readLines(tmp)
}
