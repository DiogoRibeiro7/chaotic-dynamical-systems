% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap-ci.R
\name{bootstrap_extremal_index}
\alias{bootstrap_extremal_index}
\title{Bootstrap confidence intervals for the extremal index}
\usage{
bootstrap_extremal_index(
  x,
  threshold,
  estimator = c("runs", "intervals"),
  run_length = 5L,
  block_size = 50L,
  B = 1000L,
  parallel = FALSE
)
}
\arguments{
\item{x}{Numeric vector of observations.}

\item{threshold}{Numeric threshold for exceedances.}

\item{estimator}{Character string, either "runs" or "intervals".}

\item{run_length}{Integer run parameter for the runs estimator.}

\item{block_size}{Integer block length for resampling.}

\item{B}{Integer number of bootstrap replicates.}

\item{parallel}{Logical. If TRUE use parallel processing via
[parallel::mclapply()].}
}
\value{
A list with elements `theta_hat`, the point estimate; `replicates`,
  the bootstrap sample of extremal index estimates; and `ci`, the 95%
  percentile confidence interval. Returns `NA` values in the replicates if
  the estimator fails on a resampled series.
}
\description{
Implements a simple block bootstrap for the extremal index
using either the runs or intervals estimator.
}
\examples{
set.seed(123)
x <- arima.sim(model = list(ar = 0.7), n = 2000)
thr <- quantile(x, 0.95)
boot <- bootstrap_extremal_index(x, thr, estimator = "runs",
                                 run_length = 5, block_size = 50, B = 200)
boot$ci
}
