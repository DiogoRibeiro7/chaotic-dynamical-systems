% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom-methods.R
\name{augment.chaotic_model}
\alias{augment.chaotic_model}
\title{Augment a chaoticds GEV or GPD fit with fitted CDF / survival columns}
\usage{
\method{augment}{chaotic_model}(x, data = NULL, ...)
}
\arguments{
\item{x}{A `chaotic_model` returned by [fit_gev()] or [fit_gpd()].}

\item{data}{Optional numeric vector. If `NULL` (default), the data
stored in the fit are used. For GPD this should be the *raw* series
(the function will subset to exceedances above the threshold).}

\item{...}{Unused.}
}
\value{
A data frame with columns `index`, `value`, `cdf`, `survival`,
  ordered to match the input.
}
\description{
Returns the input data alongside model-implied CDF and survival
(exceedance-probability) values at each observation. Useful for
PP / QQ-style diagnostic plots.
}
\examples{
set.seed(1)
fit <- fit_gev(evd::rgev(500, 0, 1, 0.1))
head(augment(fit))

}
