% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_cat_map}
\alias{simulate_cat_map}
\title{Simulate the Arnold cat map}
\usage{
simulate_cat_map(n, x0 = 0.1, y0 = 0.1, noise_sd = 0)
}
\arguments{
\item{n}{Integer. Number of iterations to generate.}

\item{x0}{Numeric. Initial x value. Defaults to 0.1.}

\item{y0}{Numeric. Initial y value. Defaults to 0.1.}

\item{noise_sd}{Numeric (\eqn{\ge 0}). Standard deviation of additive
Gaussian noise applied to each component after each iteration; the
modulo is re-applied after perturbation. Defaults to 0
(deterministic).}
}
\value{
Data frame with columns `x` and `y` of length `n`.
}
\description{
Generates an orbit for the two-dimensional Arnold cat map:
  x[n+1] = (x[n] + y[n]) mod 1
  y[n+1] = (x[n] + 2 * y[n]) mod 1
}
\examples{
orbit <- simulate_cat_map(100)
}
