% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile-likelihood.R
\name{profile_ci}
\alias{profile_ci}
\title{Profile-likelihood confidence intervals for a GEV or GPD fit}
\usage{
profile_ci(fit, parameter = NULL, level = 0.95, ...)
}
\arguments{
\item{fit}{A `chaotic_model` returned by [fit_gev()] or [fit_gpd()].}

\item{parameter}{Character vector of parameter names. If `NULL`
(default), all model parameters are profiled.}

\item{level}{Confidence level. Defaults to 0.95.}

\item{...}{Passed to [profile_likelihood()] (e.g. `n_points`, `span`).}
}
\value{
A data frame with columns `parameter`, `estimate`, `lower`,
  `upper`. Either CI endpoint is `NA` if the profile curve did not
  bracket the LRT cutoff within the grid span.
}
\description{
Convenience wrapper that calls [profile_likelihood()] for each parameter
(or a chosen subset) and returns a tidy data frame.
}
\examples{
set.seed(1)
x  <- evd::rgev(500, loc = 0, scale = 1, shape = 0.1)
fit <- fit_gev(x)
profile_ci(fit)

}
