% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom-methods.R
\name{tidy.chaotic_model}
\alias{tidy.chaotic_model}
\title{Tidy a chaoticds GEV or GPD fit}
\usage{
\method{tidy}{chaotic_model}(x, conf.int = FALSE, conf.level = 0.95, ...)
}
\arguments{
\item{x}{A `chaotic_model` returned by [fit_gev()] or [fit_gpd()].}

\item{conf.int}{Logical. If `TRUE`, attach Wald confidence intervals.
Defaults to `FALSE`. For better-calibrated intervals on the shape
parameter use [profile_ci()] instead.}

\item{conf.level}{Numeric in (0, 1). Confidence level when `conf.int`
is `TRUE`. Defaults to 0.95.}

\item{...}{Unused.}
}
\value{
A data frame with columns `term`, `estimate`, `std.error`, and
  (when `conf.int = TRUE`) `conf.low`, `conf.high`.
}
\description{
Returns one row per fitted parameter with point estimates and (when the
fit reports them) Wald standard errors.
}
\examples{
set.seed(1)
fit <- fit_gev(evd::rgev(500, 0, 1, 0.1))
tidy(fit)

}
