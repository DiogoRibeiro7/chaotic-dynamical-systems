% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_ikeda_map}
\alias{simulate_ikeda_map}
\title{Simulate the Ikeda map}
\usage{
simulate_ikeda_map(n, u = 0.9, x0 = 0, y0 = 0, noise_sd = 0)
}
\arguments{
\item{n}{Integer. Number of iterations to generate.}

\item{u}{Numeric. Dissipation parameter, typically in (0, 1). Defaults
to 0.9 (chaotic regime).}

\item{x0}{Numeric. Initial x. Defaults to 0.}

\item{y0}{Numeric. Initial y. Defaults to 0.}

\item{noise_sd}{Numeric (\eqn{\ge 0}). Standard deviation of additive
Gaussian noise applied to each component after each iteration.
Defaults to 0 (deterministic).}
}
\value{
Data frame with columns `x` and `y` of length `n`.
}
\description{
Iterates the two-dimensional Ikeda map, which models the light field in
a nonlinear ring cavity:
\deqn{t_n = 0.4 - 6 / (1 + x_n^2 + y_n^2),}
\deqn{x_{n+1} = 1 + u (x_n \cos t_n - y_n \sin t_n),}
\deqn{y_{n+1} = u (x_n \sin t_n + y_n \cos t_n).}

With the standard parameter `u = 0.9` the trajectory traces a strange
attractor with a teardrop-shaped support.
}
\examples{
orbit <- simulate_ikeda_map(5000, u = 0.9)
plot(orbit$x, orbit$y, pch = ".",
     xlab = "x", ylab = "y",
     main = "Ikeda map (u = 0.9)")

}
\references{
Ikeda, K. (1979). Multiple-valued stationary state and its instability
of the transmitted light by a ring cavity system. *Optics
Communications*, 30(2), 257-261.
\doi{10.1016/0030-4018(79)90090-7}
}
\seealso{
[simulate_henon_map()], [simulate_lorenz()].

Other simulation functions: 
\code{\link{simulate_duffing}()},
\code{\link{simulate_logistic_map}()},
\code{\link{simulate_lorenz}()},
\code{\link{simulate_mackey_glass}()},
\code{\link{simulate_rossler}()},
\code{\link{simulate_standard_map}()}
}
\concept{simulation functions}
