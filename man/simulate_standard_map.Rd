% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_standard_map}
\alias{simulate_standard_map}
\title{Simulate the Chirikov standard map}
\usage{
simulate_standard_map(n, K = 1.2, p0 = 1, theta0 = 1)
}
\arguments{
\item{n}{Integer. Number of iterations to generate.}

\item{K}{Numeric. Kick strength. Defaults to 1.2 (well into the chaotic
regime).}

\item{p0}{Numeric. Initial momentum in \eqn{[0, 2\pi)}. Defaults to 1.}

\item{theta0}{Numeric. Initial angle in \eqn{[0, 2\pi)}. Defaults to 1.}
}
\value{
Data frame with columns `p` and `theta` of length `n`.
}
\description{
Iterates the area-preserving Chirikov-Taylor map on the torus
\eqn{[0, 2\pi)^2}:
\deqn{p_{n+1} = (p_n + K \sin \theta_n) \bmod 2\pi,}
\deqn{\theta_{n+1} = (\theta_n + p_{n+1}) \bmod 2\pi.}

The parameter `K` controls the strength of the nonlinear kick. The map
is integrable at `K = 0`, the last invariant KAM torus disappears near
the Chirikov value `K \approx 0.971635`, and the dynamics are
progressively more chaotic for larger `K`.
}
\examples{
orbit <- simulate_standard_map(2000, K = 1.2)
plot(orbit$theta, orbit$p, pch = ".",
     xlab = expression(theta), ylab = "p",
     main = "Standard map (K = 1.2)")

}
\references{
Chirikov, B. V. (1979). A universal instability of many-dimensional
oscillator systems. *Physics Reports*, 52(5), 263-379.
\doi{10.1016/0370-1573(79)90023-1}
}
\seealso{
[simulate_henon_map()], [simulate_cat_map()].

Other simulation functions: 
\code{\link{simulate_duffing}()},
\code{\link{simulate_ikeda_map}()},
\code{\link{simulate_logistic_map}()},
\code{\link{simulate_lorenz}()},
\code{\link{simulate_rossler}()}
}
\concept{simulation functions}
