# Select High Threshold via Quantile

Select High Threshold via Quantile

## Usage

``` r
select_threshold(X, prob = 0.99)
```

## Arguments

- X:

  Numeric vector. Time series.

- prob:

  Numeric in (0,1). Exceedance probability level.

## Value

Numeric scalar. Threshold u.
