# Fast cluster size computation (C++ implementation)

Efficient C++ implementation for computing cluster sizes of exceedances.

## Usage

``` r
cluster_sizes_cpp(exceedance_indices, run_length)
```

## Arguments

- exceedance_indices:

  Integer vector of exceedance indices

- run_length:

  Maximum gap between exceedances in same cluster

## Value

IntegerVector of cluster sizes
